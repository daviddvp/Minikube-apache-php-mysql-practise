apiVersion: v1
kind: Service
metadata:
  name: apache-bbd-service
spec:
  selector:
    app: apache-bbd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort